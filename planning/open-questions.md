# Open Questions

Pending decisions: insufficient signal to commit at current design depth.

## Q2. Desktop UI specifics
R10.2 commits to theatron-or-equivalent as v1. The actual interaction model  -  tab strip vs. command palette vs. spatial canvas vs. something agent-native  -  has no committed design. Requires prototyping, not specification.

## Q3. Agent-proposed rule review workflow
R9.7 requires the capability. The review queue UX and merge ceremony have no committed design.

## Q4. Cost accounting unit normalization
R9.4 requires cost accounting in time, fetches, tokens, and operations budget. Whether these collapse to a single normalized currency or remain separate dimensions has no committed design.

## Q5. Co-tenancy UX
R2.5 requires the operator to watch an agent work on the same session in real time. Surfacing real-time agent activity without overwhelming the operator has no committed design.
