[package]
name = "xenos"
description = "Independent Dioptron wire client for process-level acceptance tests."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
# WHY: a test client, not a supported consumer library.
publish = false

[dependencies]
ed25519-dalek.workspace = true
getrandom.workspace = true
snafu.workspace = true
# WHY path without version: publish = false; deny.toml allows wildcard
# paths for this crate.
syntheke = { path = "../syntheke" }

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
